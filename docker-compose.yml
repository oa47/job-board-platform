version: '3.8'

services:
  web:
    build: .
    command: python manage.py runserver 0.0.0.0:8000
    volumes:
      - .:/app
    ports:
      - "8000:8000"
    env_file:
      - .env
    # If testing postgres locally instead of Supabase, uncomment below:
    # depends_on:
    #   - db

  # Local Postgres fallback if not using Supabase
  # db:
  #   image: postgres:15
  #   volumes:
  #     - postgres_data:/var/lib/postgresql/data/
  #   environment:
  #     - POSTGRES_USER=postgres
  #     - POSTGRES_PASSWORD=postgres
  #     - POSTGRES_DB=jobboard
  #   ports:
  #     - "5432:5432"

# volumes:
#   postgres_data:
